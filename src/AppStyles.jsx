import styled from 'styled-components';

const AppContainer = styled.div`
  margin-top: 100px;
`;

const PagesContainer = styled.div`
  padding: 0 16px;
`;

export {
  AppContainer,
  PagesContainer
}